---
description: Run a design request through the governed adversarial UX loop
argument-hint: "<the design problem, request, or flow>"
---

# /adversarial-ux

Run $ARGUMENTS through the full loop: generate, record, blind review, human gate.

Read `skills/adversarial-ux/SKILL.md` and follow it exactly. Start at Step 0.

Do not shortcut to a design. Even a request that arrives fully specified goes through the
altitude check, because a fully specified request is usually a fully specified solution.
