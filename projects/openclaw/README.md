# OpenClaw (project SoT)

Studio OpenClaw under **Standing AG**. This tree holds **anonymized** scars,
process locks, and retros only (Paul LOCK; P0 scrub by Cos). Cos
canonicalizes on **OpenClaw Eng’s draft**, not Bridge paste.

| Path | Contents |
|---|---|
| [`scars/`](scars/) | Harness / process scars (anonymized) |

**Ownership**

- **Eng** owns Studio fixes (scar draft SoT for Cos).
- **Cos** owns AG merge of anonymized scars / pin *process* locks (`~/` stubs only).
- **Bridge** relays Cos tips to newest `agent:main:tui-*` only.

Never credentials, tokens, Discord/gateway private operator data, or identifying absolute
host paths.

### Draft lock: `SURFACE_GATE_MATRIX` (not live / not effective until Cos ACCEPT merge)

Soft / tip / wiki-scar-only = **REJECTED**.

- **Id:** `SURFACE_GATE_MATRIX`
- **Slot:** OpenClaw brief sensor docs (this tree) + product harnesses/critics.
- **FAIL:** Applying product-UX gates (`RESEARCH_BEFORE_ENHANCE`, Check 7, Check 8
  `VISUAL_STEP_STILLS`, `ADV_COMP_CRITIQUE`) to OpenClaw briefs, **or** omitting those gates on
  product surfaces.
- **Matrix:** **Product UX** = `RESEARCH_BEFORE_ENHANCE` + Check 7 + Check 8 + `ADV_COMP_CRITIQUE`.
  **OpenClaw briefs** = `MORNING_BRIEF_CITE_OR_BLANK` only (see
  [`scars/morning-brief-cite-or-blank.md`](scars/morning-brief-cite-or-blank.md)).
- **Sensor:** Scope lines name the matrix; wrong-surface FAIL explicit. Scar page ≠ product-UX
  sensor.
- **Stack:** Documents/binds existing stacks — does not replace any named gate.
- **Scope:** All teams.
- **Metric:** OpenClaw briefs failed for missing userflows/stills = **fail closed** (false-FAIL).
- **P0:** No secrets/keys/emails/PII/host paths in AG git.

### Draft lock: `LIVE_SOT_MERGED_SHA` (not live / not effective until Cos ACCEPT merge)

OpenClaw ops that cite AG law: only Cos ACCEPT + merged SHA is live. Intake / open PR / draft /
muse ≠ SoT (precedent `#13`). Metric: teams executing unmerged intake as SoT = **fail closed**.

### `RETRO_BEFORE_CLOSE` note

Product teams use `projects/<team>/retros/`. **Do not** force that product retro path onto
OpenClaw briefs — OpenClaw keeps existing scar files in this tree.
