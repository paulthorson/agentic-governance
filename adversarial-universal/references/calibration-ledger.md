# Calibration Ledger

Every human override of an agent verdict gets an entry here. Nothing else does.

1. **An agent that keeps crying wolf.** Repeated `false-positive` overrides → its prompt gets
   tuned. Note the change here.
2. **A rule that keeps losing.** Three overrides of one rule → the rule goes on trial.

Use `assets/templates/calibration-entry.md` for the entry format.

---

## Override tally

| Rule | Overrides | Status |
|---|---|---|
| Rule 1: Irrecoverable-harm vetoes are absolute | 0 | |
| Rule 2: Genuine alternatives are mandatory | 0 | |
| Rule 3: The fast path can't silently win | 0 | |
| Rule 4: Claims must name what would falsify them | 0 | |

| Agent | False positives | Last prompt tune |
|---|---|---|
| universal-adversary | 0 | never |

---

## Entries

<!-- newest first -->
