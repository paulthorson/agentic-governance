# Improve report — 2026-09-12

## Date

2026-09-12

## Improvements shipped

- OpenClaw morning-brief cite-or-blank scar SoT CLOSED + Studio gate live (`MORNING_BRIEF_CITE_OR_BLANK` Rule 2 A; `validate-brief-grounding.py` fail-closed before Discord) ([#9](https://github.com/paulthorson/agentic-governance/pull/9)).
- Standing lock `RESEARCH_BEFORE_ENHANCE` (Rule 2 A) + `ADV_COMP_CRITIQUE` merged after Adv PASS — hard cite-real-screens gate before brief/stories/pack; jury must open cited screens and file our-hole / competitor-hole / do-not-copy ([#10](https://github.com/paulthorson/agentic-governance/pull/10)). Soft / deferred Look gate B rejected by name.
- Standing scar SoT filed at `projects/_standing/scars/research-before-enhance.md` (cross-project UX/Research; not OpenClaw morning-brief scope).

## Gains

- Second consecutive Cos AG improve day with public SoT: one new Closed OpenClaw Discord ship-gate scar + one standing constitution/process lock (sensors named; scar page alone is not the gate).
- Invented-meeting failure mode on morning briefs now has a named fail-closed sensor (dry-run caught prior invented brief; Discord withhold on fail).
- Product UX enhance path cannot skip real-screen `evidence.md` before PM→UX brief / first story; Adv FAIL criteria explicit for soft-gate restoration.
- Candidate (still not ACCEPT): Feel / cold-play sits need a surface-alive pin or named relaunch before VO/Boss scoring (tab-discard mid organic sit / Fuse Look family). Remains Adv-challenge queue — no constitution edit today.

## KPIs

| KPI | Value | Source / method |
|---|---|---|
| Tokens | not measured yet | No attributed AG token bill wired yet |
| Cycle time | not measured yet | No start→merge timer ledger yet |
| Retros | 2 filed (unchanged) | Count of named product-seat retros under `projects/*/retros/*.md` (excl. README; paths on product briefs) on main; no new triad filed today |
| AG PRs | 2 merged today | GitHub merged PRs on 2026-09-12: #9 #10 |
| Money (optional) | — | — |

## Post-epic retros

### Filed

- named product seat — [migration-drift-gate](../../projects/) (private product retro — path on product brief) (prior)
- named product seat — [marketing-landing](../../projects/) (private product retro — path on product brief) (prior)

### Missing / overdue (flag CEOs)

- **product-seat CEO** (named product; path on product brief) — closed-epic ships (Sep 8–12) still lack formal triad retros under that product's private retros path (on product brief) (chat retro Sep 8 only). Operator feel-stamp remain UNPAID (retry missed; metric cold-play PASS still 0). Related surface HOLD through Sep 12.
- **product-seat CEO** (named product; path on product brief) — product-surface research/stories in flight (details on product brief); regional surface FAIL after prior optical PASS (details on product brief) — file AG epic retro SoT when that product epic CLOSES (not yet).
- other named product seats — no closed epics requiring new retros today (one product seat PARK through Mon Sep 14 9:00 AM ET).

Durable AG framework-policy proposals from these learnings must route **Adv challenge → Cos → operator ACCEPT** (no silent constitution edits).

## OpenClaw efficiency intake (anonymized)

Bucketed ops tips for Bridge → newest `agent:main:tui-*` only. Eng owns Studio implement. P0: no PII/secrets/tokens/absolute host paths/private operator data — `~/` stubs only.

### (1) running faster

- Cite-or-blank + fail-closed grounding gate cuts rewrite→ship loops on empty evidence (no-evidence brief instead of inventing Meeting Synthesis tables).

### (2) smoother ops

- Order locked: generate → term check → adversarial → **grounding gate** → Discord. KICK_BACK alone is not ship authority; re-ground every rewrite.
- Related ticket pattern (not this scar’s done-when): recover ≠ close on unhealthy Paperclip seats; early-warning on resume-count so silent 6am-class flaps surface.

### (3) fewer breaks / reliability

- `validate-brief-grounding.py` fail-closed before Discord; on fail Discord only withhold alert (vault kept); invented-meeting count on **shipped** briefs = fail closed.
- Temperature ≤0.3; forbid “fill verbose quota by inventing”; frontmatter citations / accurate `source_meetings`.

### (4) better skills

- Reusable cite-or-blank skill pattern for any Discord/report ship path that previously treated adversarial rewrite as grounding.

### (5) continuous improvements

- Unpaid from scar (do not drop): unify generator vs term-validator transcript trees; align generator exit-2 special-case with shell abort while keeping grounding withhold distinct.
- File Closed harness scars into CLOSED harness scar SoT (Studio private / product brief — OUT of framework repo) with well / didn’t / improve; Cos canonicalizes Eng drafts.

### (6) CI/CD

- Discord ship path now has an explicit pre-post fail-closed sensor (docs SoT + Studio gate). No AG CI package change beyond #9/#10 merges today.

**Scar / PR URLs**

- CLOSED harness scar SoT (Studio private / product brief — OUT of framework repo)
- https://github.com/paulthorson/agentic-governance/blob/main/projects/_standing/scars/research-before-enhance.md
- https://github.com/paulthorson/agentic-governance/pull/9
- https://github.com/paulthorson/agentic-governance/pull/10

## Limen / engine feedback sent

- none today

## Open contribution invites

- Spin up the public Vercel project (Root Directory `dashboard`) + Google OAuth admin secrets — still unpaid; code on main via #4/#7.
- Help wire measured token / cycle-time feeds (named source only; no fake baselines).
- Review `RESEARCH_BEFORE_ENHANCE` / `ADV_COMP_CRITIQUE` sensor language for reuse outside UX/Research.
- Try cloning AG and filing an outsider improve PR against `docs/improve/_template.md`.
- named product-seat CEOs: file triad retros when those epics CLOSE (overdue backlog noted above).

## Changelog links

- Unreleased — `RESEARCH_BEFORE_ENHANCE` + `ADV_COMP_CRITIQUE` standing lock ([#10](https://github.com/paulthorson/agentic-governance/pull/10))
- Unreleased — OpenClaw morning-brief cite-or-blank scar SoT ([#9](https://github.com/paulthorson/agentic-governance/pull/9))
- Full Unreleased block: [`CHANGELOG.md`](../../CHANGELOG.md)
