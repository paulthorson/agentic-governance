# Improve report — 2026-09-15

## Date

2026-09-15

## Improvements shipped

### Late 2026-09-14 after prior digest (carry into this day)

- **Public release hardening** Cos ACCEPT MERGED [#62](https://github.com/paulthorson/agentic-governance/pull/62) @ `445e0f0` — honest enforcement + claim alignment; Apache-2.0 + LICENSE only; no acceptance gate.
- **Check 7+8 Obsidian-class KG splash** Cos ACCEPT MERGED [#39](https://github.com/paulthorson/agentic-governance/pull/39) @ `3e8de67` — operator-approved Class B look stills path for Process Instrument craft.
- **Muse intake** Cos ACCEPT MERGED [#63](https://github.com/paulthorson/agentic-governance/pull/63) @ `9e929cb` — `docs/improve/intake/muse-2026-09-14.md` (Research absorb: three-gate close-out, Critic FAIL as gate, public numbers countable or BLANK).

### 2026-09-15 (ET)

- **Check 9 / `INITIATIVE_START_SEQUENCE` + Check 10 / `RELEASE_COMPLIANCE` + Cos memory install ASK** SoT landed Cos ACCEPT MERGED [#64](https://github.com/paulthorson/agentic-governance/pull/64) @ `a9a4327` (supersedes draft [#49](https://github.com/paulthorson/agentic-governance/pull/49)). Gothelf UX Canvas boxes 1–8 honesty absorbed. **Class A honesty tip filed** to flip CoE Draft intake → Already LIVE citing `#64` @ `a9a4327` (pays `CHECK9_10_COE_LIVE_ROW`; draft until Cos ACCEPT of honesty tip).
- **Studio `GATEWAY_SINGLE_OWNER` + `STALE_INSTALL_RECYCLE` scar** Cos ACCEPT MERGED [#65](https://github.com/paulthorson/agentic-governance/pull/65) @ `bb60bb3` — CLOSED harness scar SoT (Studio private / product brief — OUT of framework repo). Studio: handshake UP; LaunchAgent sole owner; residual session owner-prefix unpaid separate.
- **OpenClaw `QUESTION_WAIT_GUARD` scar** Cos ACCEPT MERGED [#66](https://github.com/paulthorson/agentic-governance/pull/66) @ `c70425e` — CLOSED harness scar SoT (Studio private / product brief — OUT of framework repo). Adv CONCERNs absorbed. Scope Studio session ops only — **not** Check 7/8.

### Marketing site (adjacent — not AG harness SoT)

- Site extract live at [www.agenticgovernance.app](https://www.agenticgovernance.app) (operator LOCK: OAuth/primary domain = www, not `*.vercel.app`).
- Site craft chain: [#6](https://github.com/paulthorson/agentic-governance-site/pull/6)–[#11](https://github.com/paulthorson/agentic-governance-site/pull/11); Cos craft PASS site #11 @ `14311f2` / UX line-4 PASS; operator skipped phone re-look widget; **Class B live clear still unpaid** (do not nag operator). dashboard remote-login admin unpaid (later superseded: localhost Host gate).

## Gains

- Standing AG OpenClaw self-heal family grew: pin-enforce + bridge-guard + cite-or-blank + gateway single-owner/stale-install recycle + question-wait-guard — all named CLOSED scars on AG git with Adv FAIL if later locks omit the named controls.
- Initiative start sequence + release compliance + Cos memory seating template are on main as SoT text (@ `a9a4327`); public release posture is honest (Apache-2.0 LICENSE only; no acceptance gate).
- Public face moved to dedicated site repo; AG stays framework download / feed / corpus.
- Self-audit still **not `AUDIT_CLEAR`** — `CHECK9_10_COE_LIVE_ROW` tip filed (pending Cos ACCEPT); harness draft headers, four draft locks, named product-seat triad retro (path on product brief), OAuth, site Class B live clear, session owner-prefix remain named unpaid.

## KPIs

| KPI | Value | Source / method |
|---|---|---|
| Tokens | not measured yet | No attributed AG token bill wired yet (`TOKEN_SOURCE_OR_BLANK` still draft) |
| Cycle time | not measured yet | No start→merge timer ledger yet |
| Retros | 5 filed | Count of `projects/*/retros/*.md` epic files (excl. README) on main: 2 + 3 named product-seat retros (paths on product briefs) — **no new triad retros today** |
| AG PRs | 3 merged today (ET) + 3 late-14 after prior digest | GitHub mergedAt ET: today #64 #65 #66; late-14 after 18:00 ET #62 #39 #63 |
| Merged PRs (lifetime, measured) | 53 | GitHub search `repo:paulthorson/agentic-governance is:pr is:merged` total_count at digest time |
| Open PRs (measured) | 1 | GitHub search open PRs — only draft [#26](https://github.com/paulthorson/agentic-governance/pull/26) |
| GitHub stars (measured) | 0 | GitHub API `stargazers_count` (widget gated `minVisible: 1`) |
| Money (optional) | — | — |

## Post-epic retros

### Filed

- named product seat — [migration-drift-gate](../../projects/) (private product retro — path on product brief) (prior)
- named product seat — [marketing-landing](../../projects/) (private product retro — path on product brief) (prior)
- named product seat — [epic-retro-a](../../projects/) (private product retro — path on product brief) (prior)
- named product seat — [epic-retro-b](../../projects/) (private product retro — path on product brief) (prior)
- named product seat — [epic-retro-c](../../projects/) (private product retro — path on product brief) (prior)

### Missing / overdue (flag CEOs)

- **product-seat CEO** (named product; path on product brief) — regional product-surface epic still open (details on product brief); file AG triad retro SoT under that product's private retros path (on product brief) when that epic CLOSES (folder does not exist yet).
- other named product seats — no newly closed epics requiring new retros today (one product seat PARK reopen Wed Sep 16 9:00 AM ET; no new AG triad owed from PARK alone).
- product-seat issue (private) survival MERGED LIVE (`7cc27e7` / SW 14ce) — PASS ≠ Feel; Feel OFF; Look FAIL Guest+Console unpaid; **no new triad owed until Feel/epic CLOSE**.

Durable AG framework-policy proposals from these learnings must route **Adv challenge → Cos → operator ACCEPT** (no silent constitution edits).

## SELF_AUDIT_LOOP (LIVE)

Checklist vs live scars/locks: Check 7/8 LIVE; `SELF_AUDIT_LOOP` + `RETRO_BEFORE_CLOSE` LIVE per CoE; `DESIGN_AGENCY_BAR` + `DESIGN_SYSTEM_FIRST` LIVE; Check 9 / Check 10 / Cos memory install ASK Cos ACCEPT MERGED [#64](https://github.com/paulthorson/agentic-governance/pull/64) @ `a9a4327` with Class A CoE LIVE-row honesty tip filed (pending Cos ACCEPT of tip); OpenClaw scars pin-enforce / bridge-guard / cite-or-blank / gateway-single-owner / question-wait-guard CLOSED on git; public site www.agenticgovernance.app; dashboard remote-login admin unpaid (later superseded: localhost Host gate); #26 NEVER merge (superseded by site craft); harness header drift unpaid; four draft locks unpaid; named product-seat triad retro (path on product brief) unpaid; site Class B live clear unpaid.

### Named unpaid improve items (not AUDIT_CLEAR)

1. **id:** `CHECK9_10_COE_LIVE_ROW` — **owner:** Cos / AG seat — **metric:** [#64](https://github.com/paulthorson/agentic-governance/pull/64) @ `a9a4327` merged with Cos ACCEPT claim, but `docs/CoE.md` Draft intake still labeled Check 9 / Check 10 / Cos memory **DRAFT** = **fail closed** after CoE LIVE-row cites #64 @ `a9a4327` **or** Cos reaffirm HOLD — **AC:** one PR moves Check 9/10 + Cos memory to CoE LIVE table citing merged SHA; Adv PASS if Adv needed for SoT name; Cos ACCEPT; no silent flip. **Status:** Class A honesty tip filed (this tip) — **pending Cos ACCEPT** (does not invent checks; Check 9 stays fail-closed; Check 10 stays Cos checklist not stop-the-presses; Cos memory ASK `private_git` OR `local_folder` stays).
2. **id:** `HARNESS_LIVE_CITE_DRIFT` — **owner:** Cos / AG seat — **metric:** `harnesses/chief-of-staff.md` still says “Draft SoT until Cos ACCEPT” for `SELF_AUDIT_LOOP` while CoE cites LIVE @ `5c10194` = **fail closed** after sync PR — **AC:** harness headers match CoE LIVE cites; Adv PASS; Cos ACCEPT.
3. **id:** `SITE_CLASS_B_LIVE_CLEAR` — **owner:** Cos craft → operator yes (when ready) — **metric:** www.agenticgovernance.app Class B live clear unpaid after site #11 Cos craft PASS; operator skipped re-look widget = **fail closed** when operator phone PASS or Cos documents HOLD with Eng unpaid bar — **AC:** no nag; clear only on operator yes or named HOLD with owner.
4. **id:** `DASHBOARD_ADMIN_LOCALHOST` — **owner:** Cos / operator — **metric:** `/admin` localhost Host gate only; remote identity login retired = **fail closed** when localhost admin works without remote login — **AC:** local Host allow; non-local fail closed.
5. **id:** `PRODUCT_EPIC_RETRO` — **owner:** product-seat CEO (named product; path on product brief) — **metric:** closed named-product regional epic without that product's private retros triad (path on product brief) = **fail closed** — **AC:** file well/didn't/improve when epic CLOSES.
6. **id:** `FOUR_DRAFT_LOCKS_ADV` — **owner:** Adv → Cos — **metric:** `CRITIC_SEPARATE_STAMP` / `TOKEN_SOURCE_OR_BLANK` / `LIVE_SOT_MERGED_SHA` / `SURFACE_GATE_MATRIX` remain draft — **AC:** Adv challenge each; Cos ACCEPT only if sensors improve AG; no silent LIVE flip.
7. **id:** `SESSION_OWNER_PREFIX` — **owner:** Studio Eng — **metric:** residual bare session keys need owner prefix (paperclip class); gateway JSON `defaultSessionKeyPrefix` schema-REJECTED — use owner-guard only — **AC:** Studio Ready tip; Adv challenge anonymized scar if AG locks omit the named control; P0 no PII/sauce into AG.

## OpenClaw efficiency intake (anonymized)

Bucketed ops tips for Bridge → newest `agent:main:tui-*` only. Eng owns Studio implement. Use `~/` stubs only.

### (1) running faster

- Do **not** run product UX Check 7/8 / Check 9 / userflows/stills / `DESIGN_SYSTEM_FIRST` / `DESIGN_AGENCY_BAR` / `RESEARCH_HCI` on OpenClaw briefs — only OpenClaw-scoped gates (`MORNING_BRIEF_CITE_OR_BLANK`, gateway/question guards). Wrong-surface FAIL burns tokens.
- Stale-install `UNAVAILABLE`: one LaunchAgent recycle then prove-handshake — do not open multi-path firefights in parallel.

### (2) smoother ops

- Cite merged SHA / merged PR only as live law (`LIVE_SOT_MERGED_SHA` draft; CoE practice for LIVE rows). Intake / open PR / muse / Draft intake ≠ LIVE constitution.
- Bridge catch-up: absorb Studio markers into Cos day log, then tip Eng for implement — Bridge remains middleman only.
- Cos memory seating: when Cos is seated, wizard ASK `private_git` OR `local_folder` (do not force one) — framework install/setup, not README-only.

### (3) fewer breaks / reliability

- Keep morning-brief grounding fail-closed before Discord (scar CLOSED).
- Pin-enforce + bridge-guard scars remain CLOSED — do not regress nightly global package bumps or blanket config restore.
- **`GATEWAY_SINGLE_OWNER`:** LaunchAgent only owns Studio gateway; kill competing gateway processes; never dual `gateway run` while LaunchAgent loaded ([#65](https://github.com/paulthorson/agentic-governance/pull/65) @ `bb60bb3`).
- **`STALE_INSTALL_RECYCLE`:** detect install-changed UNAVAILABLE / dist newer than PID; one `launchctl kickstart -k` of LaunchAgent; Discord/alert only after **2** handshake fails.
- **`QUESTION_WAIT_GUARD`:** if main session `blocked_tool_call` age ≥120s, auto-abort; alert only on real abort or ×2 abort failure ([#66](https://github.com/paulthorson/agentic-governance/pull/66) @ `c70425e`). Masked-token prompts must not block `agent:main:main`.
- Residual: session owner-prefix via owner-guard script only — do not hand-edit schema-rejected gateway JSON keys.

### (4) better skills

- Reuse cite-or-blank / fail-closed grounding for any report ship path.
- When filing improve intake (muse-style), keep well / didn’t / improve + strip absolute paths before AG git.
- Product UX stills / design-system / Check 9 skills stay out of OpenClaw brief scope.

### (5) continuous improvements

- Cos weekly CoE routine hygiene (kill expired one-shots; keep hourly AG PR sweep / 6pm improve / morning digest / weekday decision watch).
- File Closed harness scars into CLOSED harness scar SoT (Studio private / product brief — OUT of framework repo) with well / didn’t / improve when Studio closes a new gate (today: gateway + question-wait).
- Absorb LIVE AG cites into Studio only after Cos ACCEPT — and only for the named surface (OpenClaw ops ≠ product UX).

### (6) CI/CD

- AG repo: hourly PR sweep + validate/tests/secrets checks. Public site: https://www.agenticgovernance.app (site extract successor; not `agentic-governance.vercel.app` as primary).
- Traction feed: `data/traction.json` measured GitHub counts only (never invent).
- No new OpenClaw Studio CI package today; absorb LIVE AG cites only after Cos ACCEPT SHAs.

**Scar / PR URLs**

- CLOSED harness scar SoT (Studio private / product brief — OUT of framework repo)
- https://github.com/paulthorson/agentic-governance/blob/main/docs/CoE.md
- https://github.com/paulthorson/agentic-governance/pull/64
- https://github.com/paulthorson/agentic-governance/pull/65
- https://github.com/paulthorson/agentic-governance/pull/66
- https://github.com/paulthorson/agentic-governance/pull/62
- https://github.com/paulthorson/agentic-governance/pull/39
- https://github.com/paulthorson/agentic-governance/pull/63
- https://github.com/paulthorson/agentic-governance/pull/26

## Limen / engine feedback sent

- Muse learnings intake on git ([#63](https://github.com/paulthorson/agentic-governance/pull/63); `docs/improve/intake/muse-2026-09-14.md`) — docs/intake only; no Limen engine API feedback today.

## Open contribution invites

- Flip Check 9/10 into CoE LIVE table citing #64 @ `a9a4327` (or reaffirm HOLD) — clear `CHECK9_10_COE_LIVE_ROW`.
- Sync harness LIVE cites (`HARNESS_LIVE_CITE_DRIFT`) so CoE and harnesses agree.
- Adv challenge the four remaining draft locks before Cos proposes LIVE.
- Site Class B live clear: Eng unpaid craft bar if needed; operator yes when ready — do not nag.
- Dashboard admin remote-login path retired — localhost Host gate only.
- product-seat CEO (named product; path on product brief): file triad retro when that product epic CLOSES.
- Studio Eng: finish session owner-prefix Ready tip (schema-safe).
- Outsiders: clone AG and file an improve PR against `docs/improve/_template.md`.

## Changelog links

- Unreleased — Check 9 / Check 10 / Cos memory seating SoT ([#64](https://github.com/paulthorson/agentic-governance/pull/64) @ `a9a4327`) — CoE LIVE-row unpaid
- Unreleased — OpenClaw gateway single-owner + stale-install recycle scar ([#65](https://github.com/paulthorson/agentic-governance/pull/65) @ `bb60bb3`)
- Unreleased — OpenClaw question-wait-guard scar ([#66](https://github.com/paulthorson/agentic-governance/pull/66) @ `c70425e`)
- Unreleased (late-14) — Public release hardening ([#62](https://github.com/paulthorson/agentic-governance/pull/62) @ `445e0f0`)
- Unreleased (late-14) — Check 7+8 KG splash stills path ([#39](https://github.com/paulthorson/agentic-governance/pull/39) @ `3e8de67`)
- Unreleased (late-14) — Muse intake 2026-09-14 ([#63](https://github.com/paulthorson/agentic-governance/pull/63) @ `9e929cb`)
- Full Unreleased block: [`CHANGELOG.md`](../../CHANGELOG.md)
