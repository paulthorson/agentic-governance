# Improve report — 2026-09-11

## Date

2026-09-11

## Improvements shipped

- Continuous improve reporting path (`docs/improve/`) + localhost dashboard scaffold ([#4](https://github.com/paulthorson/agentic-governance/pull/4)).
- Public marketing + localhost metrics admin split on the localhost dashboard app (`dashboard/` public `/` + `/admin/*`) ([#7](https://github.com/paulthorson/agentic-governance/pull/7)).
- named product-seat epic retros SoT filed (paths on product brief) and MCP package noted v0.1.1 ([#3](https://github.com/paulthorson/agentic-governance/pull/3)).
- OpenClaw pin-enforce version-drift scar SoT CLOSED ([#6](https://github.com/paulthorson/agentic-governance/pull/6)).
- OpenClaw bridge-guard false-clobber scar SoT CLOSED ([#8](https://github.com/paulthorson/agentic-governance/pull/8)).
- Operating rhythm LOCKs recorded: Cos sole AG improve focus; daily 6pm ET digest; Adv challenges durable AG changes before Cos→operator ACCEPT; Standing OpenClaw intake (anonymized only).

## Gains

- First full Cos AG improve day with public SoT for retros + two Closed harness scars (not chat-only).
- Dashboard can read dated improve MD + anonymized scar index once localhost metrics admin is reachable (still unpaid at that time; now localhost Host gate).
- Standing AG allowlist path proven: Eng drafts scars → Cos merges anonymized SoT; Bridge is middleman, Eng owns Studio.
- Structural vs content check named in bridge-guard scar (Adv FAIL if future locks assume byte-exact config without that distinction).
- Pin + enforcer + no-auto-update named as one three-part lock (Adv FAIL if AG proposals assume pin-stable OpenClaw without citing it).

## KPIs

| KPI | Value | Source / method |
|---|---|---|
| Tokens | not measured yet | No attributed AG token bill wired yet |
| Cycle time | not measured yet | No start→merge timer ledger yet |
| Retros | 2 filed | Count of named product-seat retros under `projects/*/retros/*.md` (excl. README; paths on product briefs) on main after #3 |
| AG PRs | 5 merged today | GitHub merged PRs on 2026-09-11: #3 #4 #6 #7 #8 |
| Money (optional) | — | — |

## Post-epic retros

### Filed

- named product seat — [migration-drift-gate](../../projects/) (private product retro — path on product brief)
- named product seat — [marketing-landing](../../projects/) (private product retro — path on product brief)

### Missing / overdue (flag CEOs)

- **product-seat CEO** (named product; path on product brief) — closed-epic ships (Sep 8–11) still lack formal triad retros under that product's private retros path (on product brief) (chat retro Sep 8 only).
- **product-seat CEO** (named product; path on product brief) — regional bugs + optical stills work (details on product brief): no AG epic retro SoT yet (file when epic CLOSES).
- other named product seats — no closed epics requiring retros flagged today.

Durable AG framework-policy proposals from these learnings must route **Adv challenge → Cos → operator ACCEPT** (no silent constitution edits).

## OpenClaw efficiency intake (anonymized)

Bucketed ops tips for Bridge → newest `agent:main:tui-*` only. Eng owns Studio implement. P0: no PII/secrets/tokens/absolute host paths/private operator data.

### (1) running faster

- Keep intentional OpenClaw upgrades as explicit Eng work; never let nightly package update invent upgrades (stops surprise schema fights).

### (2) smoother ops

- Bridge-guard: Discord only on real `.mjs` drift, bridge-entry repair failure, or handshake ≥2 consecutive fails — not on desktop MCP config byte noise.

### (3) fewer breaks / reliability

- Pin + periodic enforcer + nightly-no-auto-update as one lock (`~/…/pins/` stub only; cite scar).
- Structural check for `mcpServers["openclaw-bridge"]` entry (command + args ending in bridge `server.mjs`); per-file `.mjs` restore; never restore-all from sibling config rewrite.

### (4) better skills

- none new today beyond scar SoT as reusable process templates.

### (5) continuous improvements

- File Closed harness scars into CLOSED harness scar SoT (Studio private / product brief — OUT of framework repo) with well / didn’t / improve; Cos canonicalizes Eng drafts (not Bridge paste).

### (6) CI/CD

- none measured today (no AG CI change beyond docs/dashboard merges).

**Scar URLs**

- CLOSED harness scar SoT (Studio private / product brief — OUT of framework repo)
- https://github.com/paulthorson/agentic-governance/pull/6
- https://github.com/paulthorson/agentic-governance/pull/8

## Limen / engine feedback sent

- none today

## Open contribution invites

- Help wire measured token / cycle-time feeds (named source only; no fake baselines).
- Review scar Adv-gate language (structural vs content; pin three-part lock) for reuse in other harnesses.
- Spin up the public Vercel project with Root Directory `dashboard` (still unpaid at that time). Admin remote-login later retired for localhost Host gate.
- Try cloning AG and filing an outsider improve PR against `docs/improve/_template.md`.

## Changelog links

- Unreleased — OpenClaw bridge-guard false-clobber scar SoT ([#8](https://github.com/paulthorson/agentic-governance/pull/8))
- Unreleased — Dashboard public + admin split ([#7](https://github.com/paulthorson/agentic-governance/pull/7))
- Unreleased — OpenClaw pin-enforce scar SoT ([#6](https://github.com/paulthorson/agentic-governance/pull/6))
- Unreleased — Continuous improve reporting path + dashboard scaffold ([#4](https://github.com/paulthorson/agentic-governance/pull/4))
- Unreleased / v0.1.1 note — named product-seat epic retros SoT (paths on product brief) ([#3](https://github.com/paulthorson/agentic-governance/pull/3))
- Full Unreleased block: [`CHANGELOG.md`](../../CHANGELOG.md)
